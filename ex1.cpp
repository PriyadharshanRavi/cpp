#include <iostream> /* So Basically iostream helps in processing the source file before it gets compiled*/
int main(){
    using namespace std; //basically tells the c++ to use all the basic standards of c++
    cout << "Corona Virus";
    cout << endl; // ending the above line & starting a new line
    cout << "It is an Air-borne Disease" << endl;
    // system("pause");  pause the system and look at the function or it basically tells the compiler to pause the run screen before quiting
    return 0; // end the main function 
}